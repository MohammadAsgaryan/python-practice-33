# Python Web Scraper

A simple Python web scraper that extracts news titles from a webpage and saves them into `news.csv`.

## 📌 Features
✔ Fetch webpage content using `requests`  
✔ Extract titles using `BeautifulSoup`  
✔ Save data into CSV using `pandas`

---

## 🚀 How to Run

1. Install required packages:

```bash
pip install -r requirements.txt


Run the script:

python scraper.py


After execution, a file named news.csv will be generated.

✅ Technologies Used

Python

requests

BeautifulSoup (bs4)

pandas


📝 Output
عنوان خبر
نمونه خبر ۱
نمونه خبر ۲


⚠️ Notes

Some websites may block scraping

Use proxies or headers if needed

Always respect site rules (robots.txt)